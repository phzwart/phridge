JSON Schema generated from `schema/phridge.yaml` (`make schema-docs`).

These check **JSON metadata** only. Packed npz buffers and i_seq identity
are validated in Python pack constructors and `model_geometry`.
